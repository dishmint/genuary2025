/* -------------------------------------------------------------------------- */
/*                                PROCESSING 4                                */
/* -------------------------------------------------------------------------- */
// TITLE: gen25-02
// AUTHOR: Faizon Zaman

import processing.javafx.*;

void setup() {
	size(400,400, FX2D);
	pixelDensity(displayDensity());
	hint(ENABLE_STROKE_PURE);
	surface.setTitle("gen25-02");
	
	rectMode(CENTER);
	imageMode(CENTER);

	background(0);
}

void draw() {
	background(0);
	
}